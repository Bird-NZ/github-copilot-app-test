# API Backend URL (set at build time or runtime via Container App env var)
REACT_APP_API_URL=https://zd-ca-api-dev-aue.australiaeast.azurecontainerapps.io

# Application Insights Connection String (injected via Container App secret)
APPLICATIONINSIGHTS_CONNECTION_STRING=

# Environment
ENVIRONMENT=dev