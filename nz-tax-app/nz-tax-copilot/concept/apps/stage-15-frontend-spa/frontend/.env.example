# API Backend URL (set at build time or runtime via Container App env var)
VITE_API_URL=http://localhost:8787

# First release runs with auth disabled until Azure AD B2C is reintroduced
VITE_AUTH_MODE=none

# Environment
VITE_ENVIRONMENT=dev
