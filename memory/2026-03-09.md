# 2026-03-09

- Restored Bender’s WhatsApp group behavior for **WLG Lads** by re-adding the specific group JID override `64274382968-1522024842@g.us` with `requireMention: false` in `~/.openclaw/openclaw.json`, then restarting the gateway. This fix got Bender responding in the chain group again.
- Tuned Bender’s persona and coaching system substantially:
  - loosened tone to allow more swearing/roasting while still avoiding genuinely harmful behavior,
  - added explicit guidance for more human-feeling interjections,
  - created `workspace-bender/GROUP_COACHING.md`,
  - upgraded the daily group audit to capture **good / bad / missed** examples and allow small targeted persona edits.
- Important clarification from Mat: Bender should feel like **Futurama Bender**, not a generic humanized assistant. The target is recognisably Bender through attitude, sarcasm, ego, and timing — without overusing clunky “I am a robot” signalling.
- Mat asked for continuous, ongoing improvement of Bender’s chat behavior. A systematic coaching loop is now desired by default: observe real group interactions, judge misses/landed jokes, and keep refining until Bender feels socially believable.
- Mat prefers internal tool/edit failures to be silently recovered when possible. If an edit fails but can be retried/fixed, do that without surfacing raw failure noise unless it actually blocks progress.
- Clarified skill categories with Mat:
  - OpenClaw has a large bundled skill set,
  - only a smaller subset may be active in a given session,
  - Mat wanted the bundled list and descriptions surfaced in chat for selection.
- Mat selected these bundled skills as wanted/important: **blogwatcher, goplaces, session-logs, summarize, weather, xurl**.
- Voice-reply investigation outcome:
  - previous WhatsApp playable voice-note flow had been confirmed in memory earlier,
  - current machine did not have the necessary TTS/conversion path exposed,
  - Mat correctly recalled that the old path was likely Microsoft-style rather than ElevenLabs,
  - Edge TTS was identified as the likely missing path.
- Completed voice tooling install without user follow-up:
  - installed Edge TTS into `/home/mat/.openclaw/tools/edge-tts-venv`,
  - installed user-space ffmpeg via `imageio-ffmpeg`,
  - verified text -> mp3 generation and mp3 -> ogg/opus conversion both work,
  - recorded the working paths in `TOOLS.md`.
- Mat wants replies read-friendly/voice-friendly while driving; short spoken-style replies are preferred in that situation.
- New requested future capability: create a separate WhatsApp-shareable agent for collaborative business-idea work with a friend. Not built yet; requirements gathering started (name, vibe, reply style, memory, etc.).
