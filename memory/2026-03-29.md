# 2026-03-29

- Overnight NZ Tax App build workflow defect identified: DevClaw/HAL completed Slice 2E and stopped, even though Mat had explicitly asked for continuous overnight delivery through the queue.
- Durable fix applied so this class of failure is treated as a queue-continuity defect rather than a normal slice closeout:
  - added `docs/runbooks/overnight-build-continuity.md`
  - upgraded `docs/runbooks/delegated-build-protocol.md` with explicit queue-state tracking and a queue continuity rule
  - upgraded `docs/templates/build-update-template.md` to require `queue`, `queue state`, and `next slice` fields for continuous runs
  - upgraded `HEARTBEAT.md` so stopped-after-one-slice overnight queues are flagged and resumed/escalated instead of silently accepted
  - added a workspace operating rule in `AGENTS.md` and promoted the preference into `MEMORY.md`
- New durable rule: for overnight/continuous coding requests, the unit of execution is the full queue, not the current slice. After each completed slice HAL must either start the next slice immediately, mark the queue complete, or state the real blocker.
