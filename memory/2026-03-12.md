# 2026-03-12

- Mat confirmed desired AI update cadence as: weekday AI update at 8:00 AM, weekly deep-dive assumed Sunday 7:00 PM, and breaking-news AI alerts enabled.
- Built a new local skill at `skills/az-prototype` based on the local `Azure/az-prototype` repo in `/home/mat/.openclaw/workspace/az-prototype`; the skill covers init/design/build/deploy/status/error analysis/cost analysis and agent customization for `az prototype`.
- Mat wants to use AZ Prototype by giving plain-English business use cases and having the assistant map them to Azure components/services and indicative costs without requiring Azure-specific phrasing.
- Worked a Global Ingredients commercial decision-support scenario into an Azure architecture centered on Fabric + Databricks + Azure OpenAI, with Power BI, Data Lake, AI Search, Entra ID, Key Vault, Monitor, and optional app/portal layers.
- Important delivery lesson: generating files or viewing images in tool output is not the same as delivering them into WhatsApp. For requests like “put it on screen here,” the assistant must explicitly send the media into the chat surface and verify visibility before claiming completion.
- Mat shared a preferred architecture-diagram quality/style example: clean technical architecture slide, fewer words, clearer boundaries, and document/presentation-grade visuals rather than crowded lane diagrams. Future architecture visuals should target that style first.
- Produced multiple architecture diagram iterations and eventually delivered cleaner in-chat PNGs for two phases: `architecture-phase1-clean.png` and `architecture-phase2-clean.png` (with corresponding SVGs) in the workspace root. Phase 1 reached acceptable PowerPoint quality; Phase 2 still likely benefits from one more simplification pass.
- Mat explicitly wants a hard operating rule: always go find/install the tools needed to do the job to the highest practical standard, rather than settling for whatever is already installed. Updated `AGENTS.md` to add a "Highest-standard tools rule" and expanded the finish-the-job rule accordingly.
- Added another explicit hard rule to `AGENTS.md`: never leave a task in implied progress; if not finished, state exactly what remains and continue until done or truly blocked.
- Mat wants issue reporting to include remediation by default: when I describe a fixable issue, I should also action the corrective fix and say exactly what I changed, unless I am truly blocked.
- Mat asked to implement step one of the HAL improvement roadmap as an actual operating checklist. Updated `AGENTS.md` with a concrete definition-of-done checklist: lock in deliverable, destination, quality bar, and completion test up front; before claiming completion verify the thing was made, delivered to the requested surface, visible there, and good enough to use.
- Mat asked to implement step two as well. Updated `AGENTS.md` with a chief-of-staff rule: for non-trivial tasks, classify the work into roles/stages (research, planning, execution, review, delivery) and route work deliberately instead of doing everything in one blended pass.
- Mat asked to implement step three explicitly. Updated `AGENTS.md` with an upgrade-mode rule: before committing to a method, check whether the current toolchain can hit the requested quality bar; if not, identify the missing capability, find/install the better tool or asset safely, and use the upgraded path instead of polishing the weak one.
