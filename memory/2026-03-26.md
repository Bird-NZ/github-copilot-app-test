- DevClaw/ACP delegation debugging on 2026-03-26: `acpx` plugin was initially disabled, then enabled; `acpx` CLI was missing and successfully installed globally via `npm install -g acpx@latest`.
- Gateway logs later showed `acpx runtime backend registered` / `ready`, and direct local `acpx codex exec` worked non-interactively.
- Remaining blocker narrowed to OpenClaw `sessions_spawn(runtime:"acp")` orchestration: spawned ACP codex runs stall, while direct `acpx` works.
- Important config finding: OpenClaw ACPX plugin supports `plugins.entries.acpx.config.permissionMode`; spawned ACP runs were effectively read-only, so fix path is to set permission mode to `approve-all`, restart gateway, and rerun the smoke test.
