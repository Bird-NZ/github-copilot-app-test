# 2026-02-13

- Confirmed daily 7:00 AM weather update now targets **Whatawhata** and sends one concise morning message.
- Set up and validated Google tooling path: gog CLI installed, credentials configured, and `halclawdbot@gmail.com` Google auth working for Gmail + calendar reads.
- Installed X/Twitter skills for campaign work: `twitter` and `x-publisher`.
- Established GitHub backup automation previously; daily backup job continues to push workspace updates.
- Important preference from Mat: reduce repetitive approval friction and have the assistant install required software proactively when possible.
- Confirmed user preference again: weather should be sent **once only in the morning** (no duplicates).
- Created separate `bender` agent with its own workspace/persona for group-chat use, while direct chat with user remains `HAL`.
- WhatsApp group routing locked down to allowlist-only with **WLG Lads** as the only allowed group for group handling.
- Nano Banana Pro image generation is configured and tested successfully for live meme/image creation.
- Voice messaging flow is confirmed working: TTS generation + WhatsApp send as playable voice note.
- Bender group-reply troubleshooting: non-response persisted in **WLG Lads** despite allowlist and relaxed mention setting; config was rolled back to a prior known-working style (Bender mention patterns restored, broader group policy restored, gateway restarted).
- Important delivery constraint discovered: proactive `message send` to WhatsApp group by display name can fail; tool expects E.164/JID target (or configured fallback), so group announcements may require runtime-captured group JID.
