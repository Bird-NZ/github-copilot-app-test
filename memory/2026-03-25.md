# 2026-03-25

- Tax app execution slowdown lesson: Mat explicitly asked why progress was slow and wanted a permanent memory fix so he is not left waiting while HAL is stopped/stuck without clear evidence of code movement.
- Durable workflow correction made today:
  - analysis/status/re-anchoring do not count as build progress unless immediately followed by code/test/deploy work
  - if >10 minutes pass on an active build without a code/test/deploy command, HAL must treat the build as stalled, say so plainly, and restart with the next smallest coding slice
  - progress updates must separate shipped code, in-progress code, and non-coding overhead
  - when Mat asks for assurance that coding is happening, HAL should provide proof via touched files, diffs, tests, or commits
- Concrete trigger for this correction: HAL had paused too long between tax-app scope re-anchoring and actual implementation, causing avoidable waiting/friction for Mat.
