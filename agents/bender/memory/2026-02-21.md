- Diagnosed intermittent "HAL not responding" on WhatsApp as transport instability, not agent identity mismatch.
- Evidence from logs: recurring WhatsApp Web reconnect cycle with `status=499`, heartbeat-triggered reconnect after ~30 minutes idle, and occasional `ETIMEDOUT`/408 WebSocket errors.
- During healthy windows, inbound (`web-inbound`) and outbound (`web-outbound sent message`) both succeed.
