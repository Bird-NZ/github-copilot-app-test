## 2026-02-13 — Durable notes

- User requested two separate profiles: **HAL** (full access, hidden from groups) and **Bender** (group-facing, tool-enabled).
- Config now runs two agents:
  - `main` = HAL (`/agents/hal`)
  - `bender` = Bender (`/agents/bender`, default)
- WhatsApp routing updated:
  - Direct messages from `+64274382968` route to **HAL**.
  - Other WhatsApp chats/groups route to **Bender**.
- Bender hardening (WhatsApp-scoped) applied:
  - Deny tools: `exec`, `gateway`, `nodes`, `browser`, `canvas`, `sessions_send`, `sessions_spawn`.
  - Group replies require mention (`groups.*.requireMention=true`).
  - Mention patterns include `@bender`, `bender`, `@BENDER-ISH`.
- Operational behavior note: if Bender is in a group and not responding, mention-gating is likely the cause; explicit mention is required unless config is relaxed.
