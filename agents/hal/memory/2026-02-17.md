# 2026-02-17

- Improvement note from user: close the loop on operational errors instead of treating them as non-blocking when user-facing reliability is affected.
- Specific incident: chained `openclaw config set` execution was manually killed (`SIGKILL`), creating noisy failure events despite partial success.
- Required behavior change:
  1) Prefer single-purpose config commands over long chained commands.
  2) Avoid manual process kills unless truly stuck.
  3) Always verify applied config values after updates.
  4) Restart gateway when required and confirm post-restart validation.
  5) Report a clean pass/fail summary and remediate immediately if failures occur.
- User requested this be tracked as an overall improvement pattern.
- Calendar preference set: when user asks to create an appointment, create it on HAL's own calendar and invite the target calendar/account (do not write directly to delegated read-only calendars).
- User preference: after each implemented change, provide a concise change summary back to the user.
