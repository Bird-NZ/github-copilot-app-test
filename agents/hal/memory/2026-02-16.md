# 2026-02-16

- Implemented `web3_valuation_v1` sales-comps ingestion pipeline:
  - Added `scripts/ingest_sales.py` to normalize raw sales from `data/raw_sales/*.csv|*.json|*.jsonl` into `data/comps.csv`.
  - Added comp quality scoring (recency, tx hash, verification, wash/self-deal/bundle penalties) and deduping rules.
  - Updated `scripts/run_pipeline.py` to auto-rebuild comps and apply quality+recency weighted comp adjustments by `(tld, category)`.
  - Updated UTC timestamp generation to timezone-aware `datetime.now(timezone.utc)`.
  - Updated README with ingestion flow and run instructions.
- User preference update: work in more iterative steps, think harder, and keep refining approach to reach high problem-solving quality during implementation.
- New operating preference approved: "FAST mode with gated checkpoints".
  - Ask clarifying questions only for goal-critical ambiguity, risky/irreversible actions, or major preference forks.
  - Otherwise proceed with bounded assumptions and deliver fast pass first, then refinement pass.
  - Pause only at gates: first tangible output, before irreversible actions, final polish.
- User reiterated strict UX rule: never send raw file links/paths as the user-visible answer for media/files; always send human-usable/playable/openable output directly.
